version: "3.9"

services:
  niceapimanager-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: niceapimanager-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

